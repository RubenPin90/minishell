LEXER
steps
// look for pipe
	// if pipe is found, everything before pipe is one command
	// if pipe is not found, everything is one command
		// read every word (before pipe) into node
// look for redirection tokens

// handle quotes

// what should happen to wrong input?
//// eg. echo hell>o world